#ifndef _SETTING_
#define _SETTING_

#include <string>

// TODO Make .json with parameters which will be loaded when program is start

// Set screen size
const int SCREEN_WIDTH = 1024;
const int SCREEN_HEIGHT = 800;

// Set tile size
const int TILE_SIZE = 16;
const int ZOOM = 2;

const std::string windowName = "SLD_Tutorial";

#endif